export interface ScanData {
  scanned_at: string;
  ip_hash?: string;
  device_type?: string;
  os?: string;
  browser?: string;
  country?: string;
  city?: string;
  region?: string;
  referrer?: string;
}
